Build the Anchor program and run the full test suite. Report any compilation errors or failing tests clearly, and propose fixes for each before applying them.

Steps:
1. `cd contracts`
2. `anchor build` — report any errors
3. `anchor test` — report results
4. If anything fails, diagnose and propose a fix. Do not auto-apply fixes to the contract logic without explaining the change first, since this is a financial product.
