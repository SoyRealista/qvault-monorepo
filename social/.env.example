# X (Twitter) API credentials — from developer.x.com (an app with Read+Write).
# Use OAuth 1.0a user-context keys. NEVER commit the real .env.
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# Optional: DRY_RUN=1 to preview without posting.
DRY_RUN=
